import { ListFormatterPipe } from './list-formatter.pipe';

describe('ListFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ListFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
